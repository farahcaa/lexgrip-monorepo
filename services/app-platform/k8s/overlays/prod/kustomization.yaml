apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

configMapGenerator:
  - name: env
    behavior: merge
    envs:
      - app-platform.env

generatorOptions:
  disableNameSuffixHash: true