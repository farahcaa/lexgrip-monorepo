apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ingressroute.yaml
  - admin-secret.yaml
  - db-secret.yaml
  - ghcr-pull-secret.yaml

configMapGenerator:
  - name: scripts
    files:
      - start-keycloak.sh
